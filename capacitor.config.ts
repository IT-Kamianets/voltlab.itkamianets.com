import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'voltlab.itkamianets.com',
  appName: 'Voltlab',
  webDir: 'dist/voltlab/browser'
};

export default config;
